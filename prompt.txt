### 🔧 โครงสร้างโปรเจกต์ที่ต้องการ

- Project  
  ├── include/  
  │   └── <ชื่อ header>.h  
  ├── src/  
  │   └── <ชื่อ implementation>.cpp  
  ├── tests/  
  │   └── main.cpp // ใช้สำหรับรันทดสอบ + ให้คะแนนอัตโนมัติ  
  ├── build/ // โฟลเดอร์สำหรับ output และ object files  
  ├── Makefile  
  └── [README.md](http://readme.md/) // (อธิบายโจทย์)
  
### 🧩 สิ่งที่ต้องมีในแต่ละไฟล์:

1. **include/<ชื่อ header>.h**
   - ประกาศฟังก์ชันที่จะให้นักเรียนเขียน เช่น int lis(const vector<int>& arr);

2. **src/<ชื่อ cpp>.cpp**
   - โครงร่างของฟังก์ชันเว้นให้เติม
   - ใช้ vector และ std เท่านั้น (ไม่ใช้ไลบรารีพิเศษ)

3. **tests/main.cpp**
   - มี test case อย่างน้อย 10 ข้อ
   - แสดงผลแบบมีคะแนน (+10 ต่อข้อ), emoji ✅❌, และสรุปคะแนนสุดท้าย

4. **Makefile**
   - รองรับ build แยกไฟล์ใน include/, src/, tests/ และวางผลลัพธ์ไว้ใน build/

5. **README.md**
   - อธิบายโจทย์ (ภาษาไทย), input/output, ข้อกำหนด และตัวอย่างเล็กน้อย
   - อย่าลืมระบุวิธี execute เช่น ## วิธี execute เข้าไปที่ directory <หัวข้อที่ต้องการ> 
   รันคำสั่ง /* make run build*

🎯 จุดประสงค์คือให้ Gemini สร้างไฟล์ที่สามารถคัดลอกไปสอนและให้นักเรียนทดสอบเองได้ทันที (ไม่มี dependency พิเศษ)

ให้คั่นแต่ละไฟล์ด้วย ________________________________________

ไม่ต้องพิมพ์อย่างอื่น พิมพ์เฉพาะเนื้อหาในไฟล์เท่านั้น

คืนค่ามา 5 ไฟล์ โดยแต่ละไฟล์มีลำดับตามนี้ readme, makefile, include, main, <ชื่อ cpp>.cpp

รูปแบบนี้


readme
________________________________________
Makefile
________________________________________
include.h
________________________________________
main.cpp
________________________________________
<ชื่อ cpp>.cpp

กรุณาตอบโดยไม่ต้องใช้ backticks หรือ code block ใดๆ ให้ตอบเป็นข้อความล้วนๆ เท่านั้น

พิมพ์มาแบบพอตัด ________________________________________ ออกแล้วพร้อมเอาไปใช้ต่อได้เลย

*สำคัญ: จงปฏิบัติตามคำสั่งด้านบนนี้อย่างเคร่งครัด*